# ChromaDB Store

This directory is a generated persistent ChromaDB store created by an example that uses the relative path `./chroma`.

It contains `chroma.sqlite3` and a UUID-named data directory. Do not edit these files manually. Recreate or remove the store only when you intentionally want to reset the local learning data.
